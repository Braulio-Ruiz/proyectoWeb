// Mensaje en la consola para verificar que el archivo se cargó correctamente
console.log('Archivo main.js cargado correctamente.');

// Saludo personalizado para el usuario
console.log('¡Que disfrutes navegando por el sitio "MIPROYECTO" de Programación Web 2024!');

// Función para manejar eventos específicos (si se requieren en el futuro)
function init() {
  // Mensaje de bienvenida en la consola
  console.log('Bienvenido al sitio de SiberOS.');
  // Aquí puedes añadir cualquier otra inicialización de eventos o funciones
}

// Ejecutar la función de inicialización cuando el DOM esté listo
$(document).ready(function () {
  init();  // Llamada a la función init()
});
